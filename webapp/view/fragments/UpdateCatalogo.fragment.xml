<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:form="sap.ui.layout.form">
    
    <Dialog
        title="Modificar Registro"
        contentWidth="500px"
        resizable="true"
        draggable="true">
        
        <content>
            <form:SimpleForm
                editable="true"
                layout="ResponsiveGridLayout"
                labelSpanXL="4"
                labelSpanL="4"
                labelSpanM="4"
                labelSpanS="12">
                
                <form:content>
                    <Label text="ID Sociedad" required="true"/>
                    <Input id="updateInputIdSociedad" 
                           value= "{update>/idsociedad}" 
                           editable="false"/>
                    
                    <Label text="ID CEDI" required="true"/>
                    <Input id="updateInputIdCedi" 
                           value= "{update>/idcedi}" 
                           editable="false"/>

                    <Label text="ID Etiqueta" required="true"
                           visible="{= ${update>/parent} === true }"/>
                    <Input id="updateInputIdEtiqueta" 
                           value= "{update>/idetiqueta}" 
                           editable="false"
                           visible="{= ${update>/parent} === true }"/>
                    
                    <Label text="Etiqueta" required="true"
                           visible="{= ${update>/parent} === true }"/>
                    <Input id="updateInputEtiqueta" 
                           value= "{update>/etiqueta}"
                           visible="{= ${update>/parent} === true }"/> 
                    
                    <Label text="ID Valor" required="true"
                           visible="{= ${update>/parent} === false }"/>
                    <Input id="updateInputIdValor" 
                           value= "{update>/idvalor}" 
                           editable="false"
                           visible="{= ${update>/parent} === false }"/>

                    <Label text="Valor" required="true"
                           visible="{= ${update>/parent} === false }"/>
                    <Input id="updateInputValor" 
                           placeholder="Ej: Color Rojo" 
                           value= "{update>/valor}"
                           visible="{= ${update>/parent} === false }"/>
                    
                    <Label text="Valor Padre"
                           visible="{= ${update>/parent} === false }"/>
                    <Input id="updateInputIdValorPa" 
                           placeholder="Ej: COLORES (Opcional)" 
                           value= "{update>/idvalorpa}"
                           visible="{= ${update>/parent} === false }"/>


                    <Label text="Secuencia"/>
                    <Input id="updateInputSecuencia" type="Number" placeholder="0" value= "{update>/secuencia}"/>
                    
                    <Label text="Imagen"/>
                    <Input id="updateInputImagen" placeholder="URL de la imagen" value= "{update>/imagen}"/>
                    
                    <Label text="Ruta"/>
                    <Input id="updateInputRuta" placeholder="Ruta" value= "{update>/ruta}"/>
                    
                    <Label text="Descripción"/>
                    <TextArea
                        id="updateTextAreaDescripcion"
                        rows="3"
                        placeholder="Descripción"
                        value= "{update>/descripcion}"/>    
                    
                </form:content>
            </form:SimpleForm>
        </content>
        
        <beginButton>
            <Button
                text="Guardar"
                type="Emphasized"
                press=".onSaveUpdate"/>
        </beginButton>
        
        <endButton>
            <Button
                text="Cancelar"
                press=".onCloseUpdate"/>
        </endButton>
        
    </Dialog>
    
</core:FragmentDefinition>